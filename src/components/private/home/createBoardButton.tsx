import React from 'react';
import { Button } from '@mui/material';

export const CreateBoardButton: React.FC = () => {
  return (
    <Button variant="contained" color="primary">
      Create New Board
    </Button>
  );
};
